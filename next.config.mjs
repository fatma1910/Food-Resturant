/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
    appDir: true, // Ensure this is enabled if using the new App directory structure
  },
};

export default nextConfig;
